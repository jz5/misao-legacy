<Window 
    x:Class="NGWordsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d"
    Title="NG Words (RegEx)" 
    Icon="application.ico"
    Width="300" Height="300" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <TextBox AcceptsReturn="True" x:Name="NGWordsTextBox" />
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 5 0 0" HorizontalAlignment="Right">
            <Button Content="OK" HorizontalAlignment="Right" Width="80" Click="OkButton_Click" IsDefault="True" />
            <Button Content="Cancel" Margin="5 0 0 0" Width="80" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
        
    </Grid>
</Window>